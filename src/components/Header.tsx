import React from "react";
import { Icon } from "antd";

const Header = () => {
  return (
    <div>
      Header
      <span>
        <Icon type="user" />
      </span>
    </div>
  );
};

export default Header;
